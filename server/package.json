{
  "name": "server",
  "version": "1.0.0",
  "description": "Apollo GraphQL Server with Express.js",
  "main": "server.js",
  "author": "Jose Arteaga",
  "license": "MIT",
  "scripts": {
    "start": "concurrently \"nodemon server.js --exec babel-node -e js\" \"cd ../client && yarn start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "apollo-server": "^3.6.3",
    "concurrently": "^7.0.0",
    "express": "^4.17.3",
    "graphql": "^16.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "nodemon": "^2.0.15"
  }
}
